import {
  addressModelToAddressMap,
  addressToAddressModelMap,
} from './addressMapper';

const barrelObject = {
  addressModelToAddressMap,
  addressToAddressModelMap,
};

export default barrelObject;
